/**
 * Data sources (not imported by code — see server.mjs):
 *
 * - Events: `events.json` in this folder (JSON array). Loaded at startup; required.
 * - Places: nikola HTTP `GET {PLACES_API_BASE_URL}/places` when the model calls the tool.
 *
 * Do not add hardcoded events here; update `events.json` instead.
 */

export {};
